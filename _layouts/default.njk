<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="I’m Dennis Heibült, a web developer from Bremen, Germany. I’m writing about front-end architecture and workflow optimisation.">
    <title>Dennis Heibült{% if title %} — {{ title }}{% endif %}</title>
    <style>
        html {
            background-color: #fcfcfc;
            color: #222;
            font-size: 18px;
            line-height: 1.5;
        }

        @media screen and (min-width: 480px) {
            html {
                font-size: calc(16px + (20 - 16) * ((100vw - 480px) / (1480 - 480)));
            }
        }

        @media screen and (min-width: 1480px) {
            html {
                font-size: 22px;
            }
        }

        body {
            margin: 0;
        }

        h1, h2, h3, h4, h5, h6 {
            line-height: 1.25;
            font-family: sans-serif;
        }

        /* Font-sizes are based on a modular scale with a ratio of `1.2` */
        h1 {
            font-size: 1.728rem;
        }

        h2 {
            font-size: 1.44rem;
        }

        h3 {
            font-size: 1.2rem;
        }

        h4, h5, h6 {
            font-size: 1rem;
        }

        a {
            color: #2a628f;
        }

        code,
        kbd {
            padding: 0.125rem 0.25rem;
            border-radius: 0.125rem;
            font-size: 0.833em;
        }

        code {
            background-color: #eaeaea;
        }

        kbd {
            display: inline-block;
            background-color: #2a628f;
            color: #fcfcfc;
            transform: skewX(-12deg);
        }

        pre {
            padding: 0.8333rem;
            border-radius: 0.125rem;
            background-color: #eaeaea;
            border: 1px solid #d5d5d5;
            overflow-x: auto;
        }

        pre code {
            padding: 0;
            border-radius: 0;
            background-color: transparent;
            font-size: 0.833em;
        }

        .o-page-wrap {
            max-width: 40rem;
            margin-left: auto;
            margin-right: auto;
            padding-left: 1rem;
            padding-right: 1rem;
        }

        .c-page-head {
            border-bottom: 1px solid #eaeaea;
        }

        .c-site-nav {
            display: flex;
            max-width: 40rem;
            margin: 0 auto;
            padding: 0;
            padding-left: 1rem;
            padding-right: 1rem;
            list-style: none;
        }

        .c-site-nav li:not(:last-child) {
            margin-right: 2.074rem;
        }

        .c-site-nav a {
            color: inherit;
            text-decoration: none;
            line-height: 2.986rem;
            font-family: sans-serif;
        }

        .c-link-list {
            margin: 0;
            padding: 0;
            list-style: none;
            border-top: 1px solid #eaeaea;
        }

        .c-link-list li {
            border-bottom: 1px solid #eaeaea;
        }

        .c-link-list a {
            display: flex;
            justify-content: space-between;
            padding-top: 1rem;
            padding-bottom: 1rem;
            font-size: 1.2rem;
            line-height: 1.25rem;
            color: #2a628f;
            text-decoration: none;
        }

        .c-info {
            padding: 0.833rem;
            background-color: #eaeaea;
            border: 1px solid #d5d5d5;
            border-radius: 0.125rem;
        }

        @media screen and (max-width: 720px) {
            .c-link-list a {
                flex-direction: column;
            }
        }

        .c-link-list__text {
            margin-right: 0.833rem;
        }

        @media screen and (max-width: 720px) {
            .c-link-list__text {
                margin-bottom: 0.402rem;
            }
        }

        .c-link-list__meta {
            flex-shrink: 0;
            font-size: 0.833em;
        }

        .c-tldr {
            padding-top: 0.8333rem;
            padding-bottom: 0.8333rem;
            border-top: 1px solid #eaeaea;
            border-bottom: 1px solid #eaeaea;
            font-weight: bold;
        }

        .c-tldr::before {
            display: block;
            font-size: 1.2rem;
            content: "tl;dr:";
        }

        .c-page-title {
            margin-bottom: 1rem;
        }

        .c-page-title__title {
            margin-bottom: 0;
        }

        .c-page-title__meta {
            font-size: 0.8333rem;
            color: #666;
        }

        .u-visually-hidden {
            position: absolute;
            height: 1px;
            width: 1px;
            overflow: hidden;
            clip: rect(1px, 1px, 1px, 1px);
        }
    </style>
</head>
<body>
    {% include "page-head.njk" %}

    <main class="o-page-wrap">
        {% if title %}
            <header class="c-page-title">
                <h1 class="c-page-title__title">{{ title }}</h1>
                <time class="c-page-title__meta" datetime="{{ date }}">{{ publishDate }}</time>
            </header>
        {% endif %}

        {% if (lang == 'en') %}
            <p class="c-info"><a href="./de/">This article is also available in german</a>.</p>
        {% endif %}
        {% if (lang == 'de') %}
            <p class="c-info"><a href="../">Dieser Artikel ist auch in englischer Sprache verfügbar</a>.</p>
        {% endif %}

        {% if tldr %}
            <h2 class="u-visually-hidden">Post summary:</h2>
            <p class="c-tldr">{{ tldr }}</p>
            <h2 class="u-visually-hidden">Main post content:</h2>
        {% endif %}

        {{ content | safe }}
    </main>
</body>
</html>
